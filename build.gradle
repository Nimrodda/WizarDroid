buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.6.+'
    }
}

allprojects {
    version = "1.1.0-SNAPSHOT"
    group = "org.codepond"
}

subprojects {
    repositories {
        mavenCentral()
    }
}