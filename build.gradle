buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.9.+'
    }
}

allprojects {
    version = "1.1.2"
    group = "org.codepond"
}

subprojects {
    repositories {
        mavenCentral()
    }
}