buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.6.+'
    }
}

allprojects {
    version = "1.1.1"
    group = "org.codepond"
}

subprojects {
    repositories {
        mavenCentral()
    }
}